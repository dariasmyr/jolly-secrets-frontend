mutation DeleteGroup ($id: Int!) {
  deleteGroup(id: $id) {
    id
    createdAt
    pictureUrl
    name
    description
    type
    status
    events {
      status
    }
    members {
      id
      role
    }
  }
}
